var isLoggedIn = true;
var isEmailVerified = true;
var isCardValid = false;

if (isLoggedIn && isEmailVerified && isCardValid){
    console.log("allow user to purchase");
}else{
    console.log("do not allow to purchase");
}